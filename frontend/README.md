# HighlightAI Frontend

React 18 + Vite frontend for the Rugby Highlight Generation System.

## Setup

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Backend API

Configure `VITE_API_URL` in `.env` (default: `http://localhost:8000/api/v1`).
